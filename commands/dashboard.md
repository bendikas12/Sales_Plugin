---
description: Render today's sales dashboard (HubSpot activity, Gmail, meetings, pipeline) to a fixed HTML file and print a summary.
argument-hint: "[optional output path]"
---

Invoke the `sales-dashboard` skill. Pass `$ARGUMENTS` through as the output path override if provided.
